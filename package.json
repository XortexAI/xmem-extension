{
  "name": "xmem-extension",
  "version": "1.0.0",
  "description": "XMem Chrome Extension — real-time memory recall for AI conversations",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "xmem-ai": "^1.1.0"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.2.0",
    "chrome-types": "^0.1.375",
    "es-module-lexer": "^2.0.0",
    "typescript": "^5.9.2",
    "vite": "^5.4.21"
  }
}
